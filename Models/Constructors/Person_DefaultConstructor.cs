public class Person_DefaultConstructor
{
    public string? Name;
    public int? Age;

    // Every C# class has a default constructor by default.
    // It initializes the object without any parameters.
    // Be careful: this can leave fields uninitialized, which might lead to null values or unexpected behavior.
}
